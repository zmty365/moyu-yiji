# TODO

## 数据导入导出功能

给「宜忌月历·摸鱼账本」加一个数据导入导出功能，方便备份和换设备迁移。

数据都存在 `chrome.storage.local` 里，导入导出就是把这份数据整个搬进搬出。

### 导出

- [ ] 在完整页（full.html）加一个「导出数据」按钮
- [ ] 点击后读取 `chrome.storage.local` 的全部数据
- [ ] 转成 JSON，触发浏览器下载，文件名带上日期，比如 `moyu-backup-2026-08-12.json`

### 导入

- [ ] 加一个「导入数据」按钮 + 隐藏的 `<input type="file">`
- [ ] 选中 JSON 文件后读取内容并 `JSON.parse`
- [ ] 简单校验一下是不是合法 JSON，格式不对就提示一下
- [ ] 写回 `chrome.storage.local`，导入前给个确认弹窗（会覆盖现有数据）
- [ ] 导入完成后刷新页面，让数据生效

### 备注

- 先做能用的版本就行，不用考虑加密、增量合并这些复杂逻辑
- 导入采用整体覆盖策略，简单可靠